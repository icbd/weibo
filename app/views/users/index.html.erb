<% provide(:title, '所有用户') %>

<h1>用户列表</h1>

<ul class="users">
  <% @users.each do |user| %>
      <li>
        <%= gravatar_for(user, {size: 50}) %>
        <%= link_to user.name, user_path(user) %>
      </li>
  <% end %>
</ul>